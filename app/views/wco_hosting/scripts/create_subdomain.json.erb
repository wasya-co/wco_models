

{
  "Comment": "optional",
  "Changes": [
    {
      "Action": "CREATE",
      "ResourceRecordSet": {
        "Name": "<%= @app.host %>",
        "Type": "A",
        "TTL": 3600,
        "ResourceRecords": [{ "Value": "<%= @app.serverhost.public_ip %>" }]
      }
    }
  ]
}
