
development:
  clients:
    default:
      database: ish_production
      hosts:
        - localhost:27017

test:
  clients:
    default:
      database: micros_email_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1
