source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in ish_models.gemspec.
gemspec

# To use a debugger
# gem 'byebug', group: [:development, :test]
